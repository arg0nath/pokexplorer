import 'package:flutter/material.dart';

class TypeResultsPage extends StatelessWidget {
  const TypeResultsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
